<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= hotel.Name %> Details</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container">
        <h2 class="p-3">Hotel Details - <%= hotel.Name %></h2>
        <div class="row">
            <div class="col">
                <p><strong>Name:</strong> <%= hotel.Name %></p>
                <p><strong>Location:</strong> <%= hotel.Location %></p>
                <p><strong>Average Rating:</strong> <%= hotel.AvgRate %></p>
                <% if (!hotel.Rated) { %>
                    <button onclick="makeRate(1, '/hotels/<%= hotel.id %>/rate')" class="btn btn-success">Rate this hotel</button>
                <% } %>
            </div>
        </div>
        <a href="/hotels" class="btn btn-primary">Back to Hotels</a>
    </div>

    <script src="../javascripts/makeRate.js"></script>
</body>
</html>







